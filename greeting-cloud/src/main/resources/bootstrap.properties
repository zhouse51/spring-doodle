#management.endpoints.web.exposure.include=*
management:
  endpoints:
    web:
      exposure:
        include: *

spring:
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true

      config:
        discovery:
          service-id: combined-server
          enabled: true
        label: master
        profile: dev

      rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest