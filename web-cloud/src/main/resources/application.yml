server:
  port: 8080
  
spring:
  application:
    name: web
  cloud:
    config:
      label: master
      profile: dev
  zipkin:
    base-url: http://localhost:9411
#      uri: http://localhost:8888/ # single config-servrer configuration

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server